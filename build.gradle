import java.util.function.BiConsumer

/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn more about Gradle by exploring our samples at https://docs.gradle.org/8.1.1/samples
 */


tasks.register('configHooksPath', Exec) {
    commandLine 'git', 'config', 'core.hooksPath', '.githooks'
}

project.getAllTasks(true).values().flatten().forEach {
    def task = it as Task
    if ( task.name == 'compileJava' ) {
        task.dependsOn(':configHooksPath')
    }
}

